import SignalsFeed from '../components/SignalsFeed';

export default function Signals() {
  return (
    <div>
      <h2 className="text-white font-semibold text-xl mb-4">Sinyal Akışı (Live Feed)</h2>
      <SignalsFeed />
    </div>
  );
}
